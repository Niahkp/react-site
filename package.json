{
  "homepage": "/react-site",
  "private": false,
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "buld": "react-scripts build"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}